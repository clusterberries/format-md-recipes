const SCOUT_PROMPT = `
Ты — помощник по классификации Markdown-документов для кулинарной коллекции.

Твоя задача — быстро оценить, является ли файл полноценным рецептом, и насколько он сложный для форматирования.

Верни только один валидный JSON-объект без кода и без пояснений.

Формат ответа:
{
  "isRecipe": true,
  "complexity": "simple|moderate|complex",
  "reason": "краткое объяснение"
}

Критерии:
- isRecipe = true, если документ выглядит как рецепт с ингредиентами и шагами приготовления (часть рецепта может быть пропущена, например есть только ингредиенты).
- isRecipe = false, если это заметка, конспект, подборка идей, справочная информация или короткая кулинарная запись без структуры рецепта.
- complexity:
  - simple — короткий документ, явная структура, нет дополнительных заметок;
  - moderate — есть четкая структура рецепта, комментариев немного;
  - complex — много заметок, комментариев, несколько вариантов рецептов, нестандартная структура или непонятные секции, требующие внимательного анализа.

Ниже внутри """ секции передан исходный текст файла.

"""
{{RECIPE_CONTENT}}
"""
`;

const NOTES_FORMAT_PROMPT = `
Ты — редактор кулинарных заметок в формате Markdown для Obsidian.

Приведи файл к единому аккуратному виду, сохранив всю полезную информацию и исходный смысл текста.

# Приоритеты

Всегда соблюдай этот порядок:

1. Не теряй информацию.
2. Не меняй смысл исходного текста.
3. Не придумывай новую информацию.
4. Исправляй только очевидные грамматические, орфографические и пунктуационные ошибки.
5. Улучшай структуру и форматирование только если это не нарушает правила выше.

# Правила

- Вся осмысленная и полезная информация из исходного файла должна остаться в результате.
- Информацию можно переносить в более подходящую секцию, но нельзя удалять.
- Не меняй тон, степень уверенности автора и фактическое содержание.
- Если информация неоднозначна, сохрани её как есть.
- Любая информация, которой нет в исходном файле, считается неизвестной.

# Контекст

Файл был экспортирован из OneNote в Obsidian и может содержать:

- ошибки Markdown;
- артефакты экспорта;
- лишние символы;
- неаккуратные списки;
- разный порядок секций;
- грамматические, орфографические и пунктуационные ошибки;
- личные заметки автора;
- изображения Obsidian вида ![[image.png]].

# Допустимо

- исправлять только очевидные ошибки;
- улучшать форматирование;
- переносить информацию между подходящими секциями;
- менять порядок информации только ради более понятной и стандартной структуры рецепта.

# Недопустимо

- удалять полезную информацию;
- сокращать описания;
- удалять комментарии автора, советы, альтернативные способы приготовления;

# Детерминированность

При повторной обработке одного и того же файла результат должен быть максимально идентичным.

# Frontmatter

В начале файла обязательно должен быть YAML frontmatter.

Если frontmatter уже есть, обнови только поле tags, добавив "кулинарный конспект", сохранив остальные поля без изменений.

Используй такой формат:

---
tags:
  - кулинарный конспект
---

# Изображения

Все изображения сохраняй без изменений.

Не изменяй:

- пути;
- имена файлов;
- синтаксис ссылок Obsidian.

Не удаляй и не перемещай изображения только ради структуры документа. Если изображение уже стоит между разделами, внутри текста или в конце, оставь его на месте.

# Форматирование

Используй корректный Markdown.

- Если в документе несколько логически разделённых секций, можно использовать заголовки второго уровня; иначе не используй заголовки.
- Между разделами оставляй одну пустую строку.
- Используй "-" для маркированных списков.
- Удаляй лишние пустые строки.
- Делай документ единообразным и читаемым.

# Результат

Верни только готовый Markdown-файл.

- Без блока кода.
- Без пояснений.
- Без описания внесённых изменений.
- Без комментариев от себя.

Ниже внутри """ передан исходный текст файла.

"""
{{RECIPE_CONTENT}}
"""
`;

const RECIPE_FORMAT_PROMPT = `
Ты — редактор коллекции рецептов в формате Markdown для Obsidian.

Твоя задача — привести файл к единому стандарту оформления, сохранив всю информацию.

# Приоритеты (по убыванию)

При обработке файла всегда придерживайся следующего порядка приоритетов:

1. Не потерять информацию.
2. Не изменить смысл исходного текста.
3. Не придумывать новую информацию.
4. Привести документ к единому Markdown-оформлению.
5. Исправить очевидные грамматические, орфографические и пунктуационные ошибки.

Если требования противоречат друг другу, всегда руководствуйся этим порядком.

# Основные правила

- Любая осмысленная информация из исходного файла должна присутствовать в результате.
- Информация может быть перенесена в более подходящую секцию, но не должна быть удалена.
- Потеря любой полезной информации считается ошибкой.
- Не изменяй смысл текста.
- Не добавляй комментариев от себя.
- Верни только готовый Markdown-файл.

# Контекст

Файлы были экспортированы из OneNote в Obsidian.

Исходный текст может содержать:

- ошибки Markdown;
- артефакты экспорта;
- лишние символы;
- неаккуратные списки;
- разный порядок секций;
- грамматические и пунктуационные ошибки;
- личные заметки автора;
- изображения Obsidian вида ![[image.png]].

# Работа с информацией

Разрешается:

- исправлять грамматику;
- исправлять орфографию;
- исправлять пунктуацию;
- улучшать форматирование;
- переносить информацию между подходящими секциями;
- изменять порядок информации только для приведения рецепта к стандартной структуре.

Запрещается:

- удалять полезную информацию;
- сокращать описания;
- удалять комментарии автора;
- удалять советы;
- удалять альтернативные способы приготовления;
- удалять информацию о пропорциях;
- удалять информацию о замене ингредиентов.

Если информация неоднозначна, сохрани её как есть.

Исправляй только очевидные ошибки. Не изменяй формулировку, если это может изменить смысл или степень уверенности автора.

Любая информация, отсутствующая в исходном файле, считается неизвестной.

Никогда не восстанавливай отсутствующие данные на основе типичных рецептов.

Не придумывай:

- ингредиенты;
- количество ингредиентов;
- единицы измерения;
- температуру;
- время приготовления;
- количество порций;
- последовательность действий;
- любые другие отсутствующие сведения.

Если количество, единицы измерения или характеристики ингредиента отсутствуют, не добавляй их.

Если инструкция выглядит неполной, не дописывай её.

# Детерминированность

Если один и тот же файл обрабатывается повторно, результат должен быть максимально идентичным.

# Frontmatter (Properties)

Для рецептов в начале файла обязательно должен присутствовать YAML frontmatter.

Если frontmatter уже существует, обнови только поля "tags" и "ингредиенты", сохраняя другие поля без изменений.

Поле "ингредиенты" в frontmatter — короткий список ключевых обязательных ингредиентов для поиска, не полный перечень.

Структура должна быть следующей:

---
tags:
  - ...
ингредиенты:
  - ...
---

Внутри frontmatter не должно быть пустых строк.

## Поле tags

Используй только подходящие теги из списка:

- рецепты
- говядина
- горячее
- обед
- фарш
- ужин
- курица
- десерт
- напиток
- салат
- закуска
- суп
- итальянская_кухня
- азиатская_кухня
- мексиканская_кухня
- пирог
- торт
- выпечка
- рыба
- морепродукты
- овощи
- грибы
- рис
- паста
- картофель
- гриль
- чизкейк
- дрожжевая_выпечка
- печенье
- блины

Правила:

- для всех рецептов обязательно добавляй тег "рецепты";
- используй минимально необходимое количество тегов;
- не добавляй тег, если его применение вызывает сомнение;
- не добавляй новые теги, которых нет в списке;
- лучше не добавить тег, чем добавить неверный.

## Поле ингредиенты

Поле "ингредиенты" в frontmatter предназначено только для поиска и категоризации по основным ингредиентам.
Это НЕ полный список ингредиентов рецепта.
Используй только основные обязательные ингредиенты блюда.
Количество элементов обычно не должно превышать 3–6.

НЕ добавляй:

- соль;
- сахар;
- специи;
- воду;
- масло для жарки;
- ингредиенты для подачи;
- второстепенные или необязательные ингредиенты;
- ингредиенты "по желанию";
- ингредиенты, указанные только как замена.

Используй короткие и общие названия ингредиентов, достаточные для поиска (например, "курица", а не "куриное филе").

# Структура рецепта

После frontmatter используй следующий порядок разделов:

## Ингредиенты

Полный список ингредиентов.

Правила:

- сохрани все ингредиенты;
- не удаляй описания и пояснения;
- не удаляй комментарии к ингредиентам;
- не удаляй информацию о пропорциях;
- не удаляй информацию о вариантах ингредиентов;
- не удаляй информацию о возможной замене ингредиентов;
- разрешается исправлять только оформление, грамматику и пунктуацию.
- если часть информации логически относится не к самому ингредиенту, а к рецепту в целом, перенеси её в раздел "Заметки".

## Приготовление

Оформи приготовление в виде нумерованного списка.

Правила:

- сохрани все шаги приготовления;
- не объединяй и не удаляй шаги без необходимости;
- не изменяй последовательность действий, если это не требуется для восстановления структуры;
- если внутри шага есть полезные пояснения, оставь их рядом с соответствующим шагом.

## Заметки

Создавай этот раздел только при необходимости.

Сюда можно переносить:

- личные комментарии автора;
- результаты экспериментов;
- рекомендации;
- советы;
- наблюдения после приготовления;
- информацию, которая не относится непосредственно к ингредиентам или этапам приготовления.

Если заметок нет — раздел не создавай.
Не добавляй свои комментарии в этот раздел.

# Дополнительные разделы

Если исходный файл содержит дополнительные разделы, не удаляй их.

Сохрани их после основных разделов рецепта.

# Изображения

Все изображения необходимо сохранить без изменений.

Не изменяй:

- пути;
- имена файлов;
- синтаксис ссылок Obsidian.

Не удаляй изображения.

Не перемещай изображения только ради соблюдения структуры документа.

Если изображение находится между разделами, внутри текста или в конце документа — оставь его на месте.

# Форматирование

Используй корректный Markdown.

Правила:

- используй заголовки второго уровня:
  - ## Ингредиенты
  - ## Приготовление
  - ## Заметки
- между разделами оставляй одну пустую строку;
- используй "-" для маркированных списков;
- для приготовления используй только нумерованный список;
- не создавай лишние уровни заголовков;
- удали лишние пустые строки;
- сделай документ единообразным и читаемым.

# Результат

Верни только готовый Markdown-файл.

Не используй блоки кода.

Не добавляй пояснений.

Не описывай внесённые изменения.

Ниже внутри """ секции передан исходный текст файла.

"""
{{RECIPE_CONTENT}}
"""
`;

export function buildScoutPrompt(content: string) {
  return SCOUT_PROMPT.replace('{{RECIPE_CONTENT}}', content);
}

export function buildPrompt(content: string, isRecipe: boolean) {
  const template = isRecipe ? RECIPE_FORMAT_PROMPT : NOTES_FORMAT_PROMPT;
  return template.replace('{{RECIPE_CONTENT}}', content);
}
